# Правки научного руководителя

## 1. Потребление памяти: агент vs оператор (глава 2)

**Замечание:** Непонятно, почему оператор потребляет 40 МБ, а агент 64 МБ, хотя оператор обслуживает больше клиентов и должен хранить больше данных.

**Анализ:** Исходные цифры были взяты из документации двух разных продуктов HashiCorp: 64 МБ — Vault Agent, 40 МБ — Vault Secrets Operator. Разница обусловлена различиями в реализации этих продуктов, а не объёмом обслуживаемых данных. Сами токены и ключи занимают единицы килобайт, потребление памяти определяется рантаймом приложения.

**Решение:** Для упрощения и устранения путаницы используем одну и ту же цифру — 64 МБ на экземпляр — для обоих подходов. Ключевое различие в суммарном потреблении определяется количеством экземпляров (2300 агентов vs 90 операторов), а не потреблением каждого из них.

**Что изменилось:**
- Оператор: 40 МБ -> 64 МБ на экземпляр
- Память на контур: 0,7 ГБ -> 1,1 ГБ
- Суммарная память: 3,5 ГБ -> 5,5 ГБ
- В таблице сравнения обновлена строка «Память, ГБ»
- Добавлено пояснение: потребление определяется рантаймом, а не объёмом данных

## 2. Перенос тестирования из главы 3 в главу 4

**Замечание:** Надо из третьей главы убрать все вопросы, относящиеся к тестированию. Тестированию посвящена глава 4.

**Анализ:** В главе 3 были три секции тестирования: интеграционные тесты auth-jwt с DSL (листинг + скриншот покрытия), E2E-тесты оператора на Ginkgo (листинг + скриншот reconcile), тестирование библиотеки (описание + скриншот покрытия). Также столбец «Тесты» в сводной таблице компонентов и упоминание тестирования во вступлении к главе.

**Решение:** Все секции тестирования перенесены в главу 4, не удалены. Глава 4 переименована в «Тестирование и апробация» и включена в thesis.tex.

**Что изменилось в главе 3:**
- Удалена секция 3.1.8 «Интеграционные тесты с DSL» (листинг, текст, скриншот покрытия)
- Удалена секция 3.2.4 «E2E-тесты» (листинг, текст, скриншот reconcile)
- Удалена секция 3.4.6 «Тестирование библиотеки» (текст, скриншот покрытия)
- Убран столбец «Тесты» из сводной таблицы компонентов
- Убрано «а также подходы к тестированию» из вступления
- Обновлены выводы: «Следующая глава посвящена тестированию компонентов и апробации»

**Что добавлено в главу 4:**
- Глава переименована: «Тестирование и апробация»
- Секция 4.1 «Тестирование компонентов системы» с тремя подсекциями:
  - 4.1.1 Интеграционные тесты auth-jwt
  - 4.1.2 E2E-тесты оператора
  - 4.1.3 Тестирование клиентской библиотеки
- Глава включена в thesis.tex

## 3. Оператор «не укладывается в grace-период» при p=1, t=100 мс (глава 2)

**Замечание:** Почему не укладывается? 100 с < 10 мин. Надо явно сослаться на раздел, где определён grace-период.

**Анализ:** Утверждение было фактически неверным. При p=1, t=100 мс: T_обн = 1000 × 0,1 = 100 с, что меньше grace-периода 600 с. Оператор не укладывается только при t=1 с (T_обн = 1000 с > 600 с).

**Решение:** Исправлена формулировка: «не укладывается» теперь корректно относится к t=1 с. Для меньших t указано, что запас недостаточен для устойчивости к сбоям. Добавлена явная ссылка на раздел 2.7.4 (rotation-slo), где определён grace-период.

## 4. Убрать упоминание 45 000 строк кода, описывать только свой код (глава 3)

**Замечание:** Возникнет закономерный вопрос: все 45 000 строк кода написали вы? Надо описывать только код, который вы писали.

**Решение:** Убрана таблица с объёмом кодовой базы и упоминание 45 000 строк. Вместо этого во вступлении к главе перечислены компоненты, разработанные автором: микросервис auth-jwt, Kubernetes-оператор, Terraform Provider и модуль, переработка клиентской библиотеки. Акцент на том, что далее описываются ключевые решения именно этих компонентов.
