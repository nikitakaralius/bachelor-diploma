\chapter{Название третьей главы: разработка программного обеспечения} \label{ch3}

% auth-jwt
% - код генерации токена, какая библиотека используется
% - код федеративной авторизации с access группами
% - код как работают enforced claims, метод EnforceClientClaims
% - код DiagnosticContext для замера производительности + скриншот из grafana
% - эндпоинт SyncTokenSet – специально для оператора, управляет лайтаймом. Метод ShouldRefreshToken
% - код CreateSecondaryKeyStep – показать как выглядит один из шагов ротации
% - TrustedIssuersRepository – показать как устроена работа с БД через EF Core
% - Какой-нибудь небольшой код работы с клиентами авторизации
% - Пример интеграционного теста с собственным DSL – показать что тесты можно описывать кратко и лаконично даже для сложного функционала + скриншот общего покрытия тестами
% auth-jwt-operator
% - структура CR - AuthJwtConfigSpec и связанные структуры в файле
% - код функции Reconcile с объяснением
% - код sendSyncTokenSetRequests – максимальный механизм защиты. Если смогли получить хоть какой-то токен, то обязательно его запишем
% - пример e2e теста (фреймворк BDD) – запускаем kubernetes в контейнере, тестируем реальное поведение системы. Тест надо выбрать небольшой, чтобы он не был на несколько страниц
% - про получившееся время реконсила, скриншот с графана, какую степень параллелизма ставим
% terraform-provider-authjwt
% - что это такое зачем нужно
% - показать пример определения ресурса
% - рассказать, что публикуется в Artifactory, на Artifactory ссылка через источники
% auth-jwt-terraform
% - рассказать как по умному сделали заведение клиентов через local, чтобы удобнее было декларировать клиентов
% - скриншот как выглядит terraform plan
% Mindbox.Authentication
% - Атрибут MindboxAuthentication – как работает
% - Интерфейсы ITokenStore, IAuthJwtClient
% - PhysicalFileProvider для обновления секретов. Секрет монтируется как файл (показать код из helm)
% - Декларативное конфигурирование возможностей библиотеки
% - Быстрое построение ключа хеша с использованием XxHash64 и stackalloc (дать ссылку на исследования производительности и статьи)
% - Форумлы подсчета bufferTime для обновления заранее (код + мат формулы)
% - Метод IsTokenStillValid – как определяется когда обновлять токен
% - Юнит и интеграционные тесты (на примерах кода, разобранного ранее)

%\FloatBarrier % заставить рисунки и другие подвижные (float) элементы остановиться
